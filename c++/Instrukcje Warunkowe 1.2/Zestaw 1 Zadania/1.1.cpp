// Napisz program, który dla trzech boków trójk¹ta a, b i c wprowadzonych z klawiatury sprawdza, czy tworz¹
// one trójk¹t prostok¹tny (zak³adamy, ¿e a > 0, b > 0, c > 0).

#include <iostream>
#include <iomanip>
#include <math.h>

using namespace std;

int main()
{
    int a, b, c;
    cin >> a >> b >> c;
    if (pow(a, 2) + pow(b, 2) = pow(c 2))
    {
        cout << "Trojkat jest prostokatny" << endl;
    }
}
