#define _USE_MATH_DEFINES
#include <iostream>
#include <iomanip>
#include <math.h>

using namespace std;

int main()
{
    int x, u, v, sum, roznica;
    cin >> u >> v;
    sum = u + v;
    roznica = u - v;
    cout << "u + v = " << sum << endl;
    cout << "u - v = " << roznica << endl;
    cin >> x;
    return -1;
}

